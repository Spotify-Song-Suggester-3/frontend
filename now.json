{
    "version": 2,
    "name": "spotify",
    "builds": [{ "src": "spotify/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "spotify/$1" }
    ]
}